recordings/*.jsonl
!recordings/demo/
workspace/.worktrees/
node_modules/
.DS_Store
workspace/

# machine-local config, and build artifacts that embed local paths
.nearly.json
ui/replay.html

# local dev-server config
.claude/launch.json
repos.json
.claude/worktrees/

# Records built by running Nearly inside this checkout. Only the curated demo
# (priya-app) belongs in the repo; test sessions were being swept in by `git add -A`.
records/*.json
!records/priya-app*.json
ui/records/*.html
!ui/records/priya-app*.html
outside/
